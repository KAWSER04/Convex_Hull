#include<bits/stdc++.h>
using namespace std;
int main(){
    int arr[]={20,2,5,7,32},i,j,n;

    int length = sizeof(arr)/sizeof(int);

    for(j=2;j<=arr.length;j++)
        key=arr[j];

return 0;
}
